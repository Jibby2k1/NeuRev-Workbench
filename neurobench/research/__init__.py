"""Repository-level research registry and generated-story tooling.

Import the focused :mod:`neurobench.research.registry` module when registry
operations are needed. Keeping package import side-effect free also makes the
module CLI safe to execute with ``python -m``.
"""
