# Manual asset placeholders

Copy the four videos and selected exported figures here using the filenames in
[`../ASSET_MANIFEST.md`](../ASSET_MANIFEST.md). These placeholders intentionally
contain no generated binary data.

Before committing large media, check the remote file-size policy. If videos are
distributed outside Git, retain these filenames in the release archive and add
stable download links to the asset manifest.
