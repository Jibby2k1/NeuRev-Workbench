# Review Required

## Status

All non-blocked work is complete for this stage.

## Decisions

For each unresolved item include:

1. the exact question;
2. why Codex cannot decide it safely;
3. the conservative default;
4. the scientific consequence of every option;
5. direct relative paths to review artifacts;
6. outputs blocked by the decision;
7. outputs that remain valid;
8. the minimal rerun command;
9. a copy-paste response line.

Do not list already resolved items or ask the user to browse an unindexed output
directory.
