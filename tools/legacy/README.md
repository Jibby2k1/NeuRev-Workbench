# Legacy Tools

This directory contains retired scripts kept for reference while canonical
package-backed commands and builders replace older hard-coded workflows.

- `generate_review_dashboard.groovy`: superseded by the Python Neurobench
  workbench/report builders. It remains here for historical comparison only.

Active Fiji/Groovy processing scripts that are still used by
`tools/run_neuron_review_pipeline.py` remain in `tools/`.
